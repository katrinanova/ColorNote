<h2 class="auto"></h2>

  <section class="note-options">
    <form>
    <label>Move note:
    <select id="note-notebook-id" name="notebook_id">

    <% notebooks.forEach(function(book){ %>

      <% if (book.id === notebook.id) { %>
      <option value="<%= notebook.escape('id') %>" selected><%= notebook.escape("title") %></option>
      <% } else { %>
      <option value='<%= book.escape("id")%>'><%= book.escape("title")%></option>
      <% } %>
    <% }) %>
    </select>
    </label>

    <label>Change color:
    <select id="note-color" name="color">
      <% Colornote.colors.forEach(function(color){ %>
        <% if (note.get("color") === color ) { %>
          <option value="<%= color %>" selected><%= color %></option>
        <% } else { %>
          <option value="<%= color %>"><%= color %></option>
        <% } %>
      <% }) %>

    </select>
    </label>
  </form>
  </section>

  <br>
  <form id ="note-show-form">
     <input id="note-title" class="nnnote-title" type="text" name="title" value="<%= note.escape("title")%>">
     <div class="share-box">
       <p class="share-note">share note</p>
       <i class="fa fa-share-square-o" id="right-share-icon" aria-hidden="true"></i>
     </div>


     <section class="images"></section>

     <section class="files"></section>

     <input id="note-file" class="fffile" type="file" name="upload">

     <textarea id="note-body" class="nnnote-body" name="body"><%= note.escape("body")%></textarea>
     <br>

  </form>
  <br>

  <!-- <script>
    $(document).ready(function(){
     $("#right-share-icon").mouseover(function(){
         $(".share-note").css("text-decoration", "underline");
     });
     $("#right-share-icon").mouseout(function(){
       $(".share-note").css("text-decoration", "none")
     })
  });
  </script> -->
